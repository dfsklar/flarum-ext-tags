import Component from 'flarum/Component';

export default class GroupsListHeader extends Component {
  view() {
      return (
        <div>
        <div class="groups-list-header">Other Groups</div>
        <div class="comment-to-lou">(TODO: hide this group from this list.)</div></div>
    );
  }
}
